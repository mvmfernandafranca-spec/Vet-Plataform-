{selectedCondition.treatments.map((treatment, index) => (
  <div key={index} className="p-4 border rounded-lg bg-muted">
    <h4 className="font-semibold text-foreground">{treatment.medication}</h4>
    <div className="grid grid-cols-3 gap-4 mt-2 text-sm">
      <div>
        <span className="font-medium">Dosagem: </span>
        <span>{treatment.dosage}</span>
      </div>
      <div>
        <span className="font-medium">Frequência: </span>
        <span>{treatment.frequency}</span>
      </div>
      <div>
        <span className="font-medium">Duração: </span>
        <span>{treatment.duration}</span>
      </div>
    </div>
    <div className="mt-3">
      <Button
        onClick={() => setMedicationDosage(calculateDosage(treatment.medication))}
        className="bg-primary hover:bg-primary/90"
      >
        Calcular Dosagem
      </Button>
      {medicationDosage && (
        <div className="mt-2 p-3 bg-accent text-accent-foreground rounded-md">
          {medicationDosage}
        </div>
      )}
    </div>
  </div>
))}